### Milk Toast Taco Phone System!


Phone apps can be registered in `phone_apps.xml`.

each phone app has a `.py` file (Make sure its filename is the same as the apps name!) and a `.html` file for its UI.

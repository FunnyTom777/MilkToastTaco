# placeholder — was `import phone` which breaks autodiscover (no top-level `phone`).
# If this app needs phone APIs, import them as:
#   from core.systems.phone.phone import contacts_list  # etc.
# Kept empty so autodiscover can import this module without error.
